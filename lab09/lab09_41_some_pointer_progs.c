#include "../lib/helpers.h"
#include "../lib/sort.h"

// a) To sort the list of numbers through pointer
// ^ already done before in sort.c (literally just pick any sorting impl)

// b) To reverse the string through pointer
// ^ already done before in helpers.c (the reverse_string func)

int main(void) {}